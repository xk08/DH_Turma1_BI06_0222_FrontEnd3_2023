import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  /* Componente global que será renderizado dentro da DIV(root) no HTML */
  <App />
)
